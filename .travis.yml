sudo: required

language: python

branches:
  only:
  - develop

services:
  - docker

before_install:
  - docker pull totaki/rock-band-tour

before_script:
  - cd src/game

script:
  - docker run --rm $PWD:/home/user/hostcwd totaki/rock-band-tour android release
