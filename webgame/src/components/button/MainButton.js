import React from 'react';
import './MainButton.scss';

const MainButton = ({ name, avatar }) => {
    return (
        <div className="main_button">
        </div>
    )
};

export default MainButton;
